# Upstream MJCF and meshes. Not committed: the 3D model files are CC BY-SA-NC
# and this is a public repo. ./fetch_assets.sh reproduces the tree from a
# pinned commit.
assets/

__pycache__/
out/*
!out/*.png
runs/*
!runs/*.json

# scratch probes, not README numbers
runs/bench.json
runs/bench_quick.json
runs/smoke*
runs/ckpt*
runs/*.pt
!runs/policy_*.pt
